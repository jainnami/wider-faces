This folder contains results from re-running the previous analysis files with more data 
(more event categories and 40 randomly sampled images from each event category).

It also has analysis from data about occlusion, scale and pose obtained from the ground-truth annotation files.
